{
    "version": "0.2.0",
    "configurations": [
         {
            "type": "chrome",
            "request": "attach",
            "name": "Attach chrome",
            "port": 9222,
            "urlFilter": "https://localhost:44434",
            "resolveSourceMapLocations": [
                "${workspaceFolder}/**",
                "!**/node_modules/**"
            ],
            "webRoot": "${workspaceFolder}/client-app/src"
        }
    ]
}